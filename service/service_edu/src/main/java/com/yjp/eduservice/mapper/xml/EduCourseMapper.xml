<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yjp.eduservice.mapper.EduCourseMapper">

    <select id="getPublishCourseInfo" resultType="com.yjp.eduservice.entity.vo.CoursePublishVo">
        SELECT ec.id,ec.title,ec.lesson_num,ecd.description,ec.cover,ec.price,et.name as teacherName,es1.title AS oneSubject,es2.title AS twoSubject
              FROM edu_course ec LEFT JOIN edu_course_description ecd ON ec.id = ecd.id
									 LEFT JOIN edu_teacher et ON ec.teacher_id = et.id
									 LEFT JOIN edu_subject es1 ON ec.subject_parent_id = es1.id
									 LEFT JOIN edu_subject es2 ON ec.subject_id = es2.id
      where ec.id=#{courseId}
    </select>
</mapper>
